# 【ガスボンベ充填伝票管理システム】設計書

---

## 0. 文書管理情報

| 項目 | 内容 |
|---|---|
| システム名 | ガスボンベ充填伝票管理システム |
| 作成者 | システム課 石井 |
| 作成日 | 2026-09-09 |
| バージョン | 1.7 |
| 承認者 | なし |
| 関連文書 | 該当なし |

### 変更履歴

| No | 日付 | 版数 | 変更内容 | 変更者 |
|---|---|---|---|---|
| 1 | 2026-09-09 | 1.0 | 初版作成 | 石井 |
| 2 | 2026-09-09 | 1.1 | 実装時にJustDB APIの実仕様（`api_help.pdf`）と突き合わせて判明した以下の点を反映。<br>・検査ビン容量計／検査ビン使用量／充填数量はJustDB側の「数値計算」フィールドでありAPI経由の書き込みを受け付けないため、送信対象から除外（画面表示・自動計算・マイナス値チェックはアプリ内で従来どおり実施）（6.2.1）<br>・ボンベ管理テーブルの伝票ID（`field_1788918872`）は手動採番フィールド（接頭語・接尾語なし）のため、`["", "<番号>", ""]` の配列形式で送信するよう修正（6.2.2）<br>・ボンベ情報明細の初期表示行数を5行→1行に変更（11章） | 石井 |
| 3 | 2026-09-10 | 1.2 | Excel出力機能を追加。<br>・メイン画面下部に「Excel出力」ボタンを追加し、押下すると期間指定と「伝票データ出力」「ボンベデータ出力」ボタンを配置した別ウインドウを表示するよう変更（11章）<br>・伝票データ・ボンベデータそれぞれのExcel出力レイアウトと出力項目定義を新設（6.3節。出力レイアウト_伝票データ.xlsx／出力レイアウト_ボンベデータ.xlsx）<br>・JustDB「レコード複数件取得」APIの公式仕様（`api_help.pdf`）を確認し、1回のリクエストで最大100件（未指定時は10件）しか取得できない仕様と判明したため、offset／limitによるページング処理を実装（6.3.2）<br>・出力対象期間の絞り込みはアプリ側（クライアント側）で行う方針とし、JustDB側のフィルター機能（動的指定可能検索）によるサーバーサイド絞り込みは事前のパネル・フィルター作成という運用負荷が生じるため、現時点では採用しない旨を記載（6.3.2） | 石井 |
| 4 | 2026-09-10 | 1.3 | Excel出力の期間絞り込みを、アプリ側（クライアントサイド）からJustDB側フィルター（動的指定可能検索）を用いたサーバーサイド方式に変更。<br>・伝票管理テーブル用（`panel_1785400222` / `filter_1789019365`）・ボンベ管理テーブル用（`panel_1788918749` / `filter_1789019750`）のパネル・フィルターがJustDB管理画面側に作成されたため、appsettings.jsonに識別名を追加し、レコード複数件取得APIに `panelName`・`filterName`・条件上書きパラメータを付与する方式に切り替え（6.3.1、6.3.4）<br>・アプリ側での期間絞り込み（Where句によるフィルタリング）は削除し、JustDB側フィルターの結果をそのまま使用するよう変更 | 石井 |
| 5 | 2026-09-10 | 1.4 | エラー処理の点検により判明した不具合の修正、および備考欄の改行・表示に関する修正を実施。<br>・【不具合修正】伝票ヘッダー登録処理（`RegisterAsync`）で、JustDB APIがエラーレスポンスを返すケース（`JustDbApiException`）以外の通信エラー（タイムアウト・接続不可等）が捕捉されず、登録操作が無反応に見える不具合を修正。該当箇所に`catch (Exception)`を追加するとともに、登録処理・Excel出力処理それぞれの最上位にも予期しない例外を捕捉する層を追加（9章）<br>・JustDB側のパネル／フィルター設定（appsettings.json）が空の場合に分かりにくいAPIエラーになっていたため、`JustDbClient`側で事前にチェックし分かりやすいメッセージを表示するよう修正（6.3.4）<br>・【不具合修正】備考欄はWPFの複数行入力で改行時に`\r\n`（CRLF）が入力されるが、JustDB APIは複数行文字列の改行記号として`\n`のみを受け付け、CRを含む値はエラーとなる仕様のため、送信前に`\r\n`・`\r`を`\n`へ正規化する処理を追加（5.2.1）<br>・Excel出力（伝票データ）の備考列にセルの折り返し表示（WrapText）と上揃え、および列幅の上限を設定し、複数行の備考が正しく表示されるよう修正（6.3.2）<br>・入力画面の備考欄（複数行TextBox）が全TextBox共通スタイルの影響で文字が縦方向中央揃えになっていたため、上揃えに個別修正（11章） | 石井 |
| 6 | 2026-09-10 | 1.5 | 「登録」ボタン押下時に確認ダイアログを表示するよう変更。<br>・入力チェック・自動計算・マイナス値チェックがすべて通過した後、JustDB APIへの送信前に「以下の内容で登録します。よろしいですか？」という確認ダイアログ（はい／いいえ、既定はいいえ）を表示するよう追加（7章、11章）<br>・確認ダイアログには配送日・販売店・配送先・充填日・充填所とボンベ情報明細の件数を表示する<br>・ボンベ情報明細が0件（未入力）のまま登録しようとした場合は、警告アイコン付きで「※ ボンベ情報明細は0件（未入力）のまま登録します。」と明示し、担当者が入力漏れに気付けるようにする<br>・「いいえ」を選択した場合は登録処理を中断し、入力内容は保持したまま画面に戻る | 石井 |
| 7 | 2026-09-11 | 1.6 | 伝票データの参照・編集・削除機能を追加。<br>・メイン画面に「参照・編集」ボタンを追加し、押下すると別ウインドウ（参照・編集画面）を表示するよう変更（6.4章、11章）<br>・伝票IDを入力して「検索」を押すと、JustDB「フィールド値検索」（`_{fieldId}=値`）を用いて対象の伝票ヘッダー・ボンベ情報明細を取得し画面に表示する（6.4.1）<br>・表示内容を編集し「変更」を押すと、伝票ヘッダーをレコード更新（PUT）し、ボンベ情報明細は検索時点との差分（追加＝INSERT、変更＝UPDATE、画面上から削除＝DELETE）をまとめて適用する。編集内容は「変更」を押すまでJustDB側には反映しない（6.4.2）<br>・「伝票削除」ボタンで、伝票ヘッダーおよび紐づくボンベ情報明細をすべて削除する。確認ダイアログ表示後に即時実行する独立した操作とする（6.4.3）<br>・`JustDbClient`にフィールド値検索（`GetRecordsByFieldValueAsync`）とレコード更新（`UpdateRecordAsync`、PUT）を追加。フィールド値検索は実際にAPIへ読み取り専用のテストリクエストを送信し動作を確認済みだが、レコード更新（PUT）のAPI契約は`api_help.pdf`で明記を確認できなかったため、既存のレコード挿入（POST）・削除（DELETE）と同じ配列ラッパー形式（`{"records":[{"recordId":…,"record":{…}}]}`）に倣った実装とし、担当者による実際の更新操作を通じて動作を確認する方針とした（6.4.1）<br>・登録画面（`MainViewModel`）と参照・編集画面（`SlipReferenceEditViewModel`）で共通するヘッダー項目・バリデーション・送信用フィールド組み立てロジックを`SlipHeaderViewModelBase`に共通化するリファクタリングを実施<br>・「変更」がエラーなく完了した場合は画面をクリアし、続けて次の伝票をすぐに検索・編集できるようにする（一部失敗時はサーバー側の現在の状態を再表示し、内容を確認できるようにする）（6.4.2、11章） | 石井 |
| 8 | 2026-09-14 | 1.7 | Excel出力の列幅計算の不具合を修正。<br>・【不具合修正】ClosedXMLの列幅自動調整（`AdjustToContents`）は全角文字を半角文字と同じ幅として計算してしまうため、日本語テキストを含む列（備考列など）の幅が実際の表示幅より狭くなる不具合があった。改行で区切った各行ごとに「半角文字＝1、全角文字＝2」換算で幅を算出し、最長行の幅を基準に列幅を設定する独自ロジック（`SetColumnWidths`）に置き換え、伝票データ・ボンベデータ両シートの全列に適用（6.3.2、6.3.3）<br>・備考列（J列）に設定していた列幅の上限（40）は、全列共通ロジックの上限・下限（上限40・下限4）として統一 | 石井 |

---

## 1. 概要・目的

ガスボンベの充填伝票およびボンベ情報の入力を支援し、入力されたデータをJustDBへ保存するところまでをスコープとするシステムである。

利用者はガス課の事務担当者であり、PC操作に不慣れな担当者でも迷わず使えるよう、シンプルな画面構成・分かりやすい操作導線を前提として設計する。

---

## 2. 用語定義・略語

該当なし

---

## 3. 前提条件・制約事項

- **稼働環境**：Windows PC上で動作するスタンドアロンのデスクトップアプリケーション（C#）
- **実行方式**：手動起動。担当者が都度アプリを起動し、入力・登録操作を行う
- **UI方針**：PC操作に不慣れな担当者でも迷わず使えるよう、シンプルな画面構成・分かりやすい操作導線とする
- **外部連携**：JustDB REST APIとBearer認証（API-Key）で連携する
  - ホスト名：`https://iw-group.just-db.com`
  - APIキー権限：伝票管理・ボンベ管理の両テーブルに対し、追加（POST）・取得（GET）・更新（PUT）・削除（DELETE）すべて有効
- **JustDB APIリクエスト制限**

| 項目 | 標準上限 | オプション適用時 |
|---|---|---|
| 1日あたりのリクエスト数 | 5,000件 | 20,000件 |
| 1分あたりのリクエスト数 | 30件 | 60件 |
| 同時接続数 | 5 | 5 |

- 想定処理件数（1日5〜10件程度）を踏まえると、標準上限の範囲内で運用可能である

---

## 4. システム全体構成

- **処理方式**：手動起動、オンライン処理（登録操作の都度JustDB APIを呼び出す）
- **データ構成**：伝票（ヘッダー）1件に対し、ボンベ情報（明細）を複数件紐づける親子構造
- **対象テーブル**

| 論理名 | tableName |
|---|---|
| 伝票管理 | `table_1785400222` |
| ボンベ管理 | `table_1788918808` |

- **ローカルマスタファイル**

| マスタ名 | ファイル名 | 件数 | 更新頻度・方法 |
|---|---|---|---|
| 配送先マスタ | 配送先マスタ_20260908.xlsx | 95件（販売店／配送先の2列） | 年1回程度、手動でファイル差し替え |
| 充填所マスタ | 充填所マスタ_20260908.xlsx | 6件 | 配送先マスタと同様、手動更新 |

- **全体処理フロー**

1. 担当者がアプリを起動し、伝票情報・ボンベ情報を入力する
2. 「登録」ボタンを押下する
3. 必須項目の入力チェックを行う（未入力があれば、該当項目名を提示するエラーメッセージを表示し処理を中断）
4. ボンベ情報明細のうち、全項目が空欄の行を登録対象から除外する
5. 検査ビン容量計・検査ビン使用量・充填数量を自動計算する
6. 計算後の数値項目（残数量・充填数量等）がマイナスとなる場合、警告を表示し登録を中断する
7. 伝票ヘッダー情報をJustDB伝票管理テーブルへPOST登録する
8. 登録結果の`recordId`を用いて対象レコードをGET取得し、自動採番された「伝票ID」の値を取得する
9. 登録対象のボンベ情報明細を1件ずつ、伝票ID・販売店・配送先・配送日を付与してJustDBボンベ管理テーブルへPOST登録する
10. 明細登録中にエラーが発生した場合、登録済みの明細および伝票ヘッダーをDELETEで削除しロールバックする（ロールバック自体が失敗した場合はシステム課への連絡を案内する）
11. すべての登録が正常に完了した場合、入力フォームの内容をクリア（初期化）し、次の伝票をすぐに入力できる状態にする

```
[担当者操作画面]
   │ 入力（伝票ヘッダー／ボンベ情報明細）
   ▼
[入力チェック／自動計算／異常値チェック] ── 参照 ──> [ローカルマスタ]
   │                                                （配送先マスタ／充填所マスタ）
   ▼
[JustDB API]
   ├─ POST/GET 伝票管理テーブル（table_1785400222）
   └─ POST     ボンベ管理テーブル（table_1788918808）
   │
   ▼（エラー時）
[ロールバック（DELETE）／エラー表示]
```

---

## 5. 入力仕様

### 5.1 入力元一覧

| No | 入力元 | 種別（ファイル/DB/API等） | 概要 |
|---|---|---|---|
| 1 | 画面入力（伝票情報） | 画面 | 担当者が伝票ヘッダー情報を入力 |
| 2 | 画面入力（ボンベ情報） | 画面 | 担当者がボンベ情報（複数件）を入力 |
| 3 | 配送先マスタ_20260908.xlsx | ファイル | 配送先候補（販売店選択に応じて絞り込み表示） |
| 4 | 充填所マスタ_20260908.xlsx | ファイル | 充填所候補（プルダウン表示） |

### 5.2 入力項目定義

#### 5.2.1 伝票情報（ヘッダー・1回の登録単位）

| No | 項目名 | 型 | 桁数 | 必須 | 説明・備考 |
|---|---|---|---|---|---|
| 1 | 配送日 | 日付 | - | ○ | |
| 2 | 販売店 | 文字列 | - | ○ | 選択式（配送先マスタの「販売店」列、7種類） |
| 3 | 配送先 | 文字列 | - | ○ | 選択式。選択済みの「販売店」に紐づく配送先候補（配送先マスタ、計95件）に絞り込んで表示 |
| 4 | 管理方法 | 文字列 | - | ○ | ラジオボタン（「メーター」／「ボンベ」）、初期値は「ボンベ」 |
| 5 | 充填日 | 日付 | - | ○ | |
| 6 | 充填所 | 文字列 | - | ○ | 選択式（充填所マスタ、6件：大丸／大阪ガスLPG／嶋田石油／粉河ガス／ホームエネルギー／充填なし） |
| 7 | 納入数量 | 数値 | - | ○ | |
| 8 | 残数量 | 数値 | - | ○ | 小数可 |
| 9 | 検査ビン容量1 | 数値 | - | 任意 | プルダウン選択式（候補：5kg／10kg／20kg／30kg／50kg）。計算時は「kg」を除いた数値のみ使用 |
| 10 | 検査ビン本数1 | 数値 | - | 任意 | |
| 11 | 検査ビン容量2 | 数値 | - | 任意 | 検査ビン容量1に同じ |
| 12 | 検査ビン本数2 | 数値 | - | 任意 | |
| 13 | 検査ビン容量3 | 数値 | - | 任意 | 検査ビン容量1に同じ |
| 14 | 検査ビン本数3 | 数値 | - | 任意 | |
| 15 | 検査ビン残数量 | 数値 | - | 任意 | 手入力。「検査ビン使用量」の算出に使用 |
| 16 | うち耐圧充填量 | 数値 | - | 任意 | 手入力項目（自動計算ではない） |
| 17 | 備考 | 文字列（複数行） | - | 任意 | 自由入力。複数行入力欄（上揃え表示、2026-09-10 版1.4で修正。修正前は全TextBox共通スタイルの影響で縦方向中央揃えになっていた） |

自動算出項目（画面表示・登録データに含めるが、担当者は直接入力しない）は「7.2 主要ロジック詳細」を参照。

**備考欄の改行コードについて（2026-09-10 版1.4で追加）**

WPFの複数行TextBoxはEnterキー入力時に`\r\n`（CRLF）を挿入するが、JustDB APIは複数行文字列の改行記号として`\n`のみを受け付け、CR（`\r`）を含む値を送信するとエラーになる仕様（`api_help.pdf` 1.5.1 レコード（リクエスト）文字列系）。そのため、JustDB送信直前に`\r\n`・`\r`を`\n`へ正規化してから送信する（`MainViewModel.BuildSlipFields` → `NormalizeLineBreaks`）。

#### 5.2.2 ボンベ情報（明細・伝票1件に対し複数件入力）

全項目が空欄の行は登録対象外とする（「8. 除外・特殊ケースの扱い」参照）。

| No | 項目名 | 型 | 桁数 | 必須 | 説明・備考 |
|---|---|---|---|---|---|
| 1 | ボンベ記号 | 文字列 | - | 行使用時は入力 | 自由入力、大文字英字のみ |
| 2 | ボンベ番号 | 文字列 | 最大6桁 | 行使用時は入力 | 数字のみを許容する文字列項目（先頭0を含む可能性があるため文字列型とする）。数字以外が入力された場合はエラー表示し登録不可。桁数未満の場合の頭0埋めは行わない |
| 3 | ボンベ容量 | 数値 | - | 行使用時は入力 | プルダウン選択式（候補：5kg／10kg／20kg／30kg／50kg）。計算時は「kg」を除いた数値のみ使用 |
| 4 | 引渡／引取 | 文字列 | - | ○（未選択では登録不可） | ラジオボタン（「引渡」／「引取」）、初期値は未選択 |
| 5 | 検査ビン | フラグ | - | 任意 | チェックボックス（該当有無を示す独立したフラグ。ヘッダーの検査ビン容量1〜3との紐付けはなし） |
| 6 | 耐圧ビン | フラグ | - | 任意 | チェックボックス（該当有無を示す独立したフラグ） |

※ 販売店・配送先・日付（配送日）はボンベ管理テーブルにも保持するが、担当者による入力項目ではなく、伝票ヘッダーの値をシステムが自動セットする（「6.2 出力項目定義」参照）。

---

## 6. 出力仕様

### 6.1 出力先一覧

| No | 出力先 | 種別 | 概要 |
|---|---|---|---|
| 1 | JustDB 伝票管理テーブル（`table_1785400222`） | API（POST／GET／PUT／DELETE） | 伝票ヘッダー情報の登録、自動採番された伝票IDの取得、ロールバック時の削除、参照・編集画面からの検索・更新・削除（2026-09-11 版1.6で追加） |
| 2 | JustDB ボンベ管理テーブル（`table_1788918808`） | API（POST／GET／PUT／DELETE） | ボンベ情報明細の登録、ロールバック時の削除、参照・編集画面からの検索・更新・削除（2026-09-11 版1.6で追加） |

### 6.2 出力項目定義

#### 6.2.1 伝票管理テーブル

| No | fieldId | 項目名 | 型 | 編集・変換規則 | 備考 |
|---|---|---|---|---|---|
| 1 | `field_20002` | 伝票ID | - | JustDB側で自動採番。登録（POST）時は送信しない | 登録後、GETで値を取得しボンベ管理側の連携キーとして使用 |
| 2 | `field_20003` | 配送日 | 日付 | 入力値をそのまま送信 | |
| 3 | `field_20004` | 販売店 | 文字列 | 選択値をそのまま送信 | |
| 4 | `field_20005` | 配送先 | 文字列 | 選択値をそのまま送信 | |
| 5 | `field_20006` | 管理方法 | 文字列 | 「メーター」または「ボンベ」 | |
| 6 | `field_20007` | 充填日 | 日付 | 入力値をそのまま送信 | |
| 7 | `field_20008` | 充填所 | 文字列 | 選択値をそのまま送信 | |
| 8 | `field_20009` | 納入数量 | 数値 | 入力値をそのまま送信 | |
| 9 | `field_20010` | 残数量 | 数値 | 入力値をそのまま送信 | |
| 10 | `field_20011` | 検査ビン容量1 | 数値 | 「kg」を除いた数値のみ送信 | |
| 11 | `field_20012` | 検査ビン本数1 | 数値 | 入力値をそのまま送信 | |
| 12 | `field_20013` | 検査ビン容量2 | 数値 | 「kg」を除いた数値のみ送信 | |
| 13 | `field_20014` | 検査ビン本数2 | 数値 | 入力値をそのまま送信 | |
| 14 | `field_20015` | 検査ビン容量3 | 数値 | 「kg」を除いた数値のみ送信 | |
| 15 | `field_20016` | 検査ビン本数3 | 数値 | 入力値をそのまま送信 | |
| 16 | `field_20018` | 検査ビン残数量 | 数値 | 入力値をそのまま送信 | |
| 17 | `field_20021` | うち耐圧充填量 | 数値 | 入力値をそのまま送信（手入力） | |
| 18 | `field_20022` | 備考 | 文字列 | 入力値をそのまま送信 | |
| 19 | `field_1785401257` | 検査ビン容量計 | 数値計算 | **送信対象外**（JustDB側の「数値計算」フィールドのためAPI経由の書き込みを受け付けない。アプリ画面では検査ビン容量1×本数1＋検査ビン容量2×本数2＋検査ビン容量3×本数3を算出して表示のみ行う） | 2026-09-09 版1.1で変更 |
| 20 | `field_1785401362` | 検査ビン使用量 | 数値計算 | **送信対象外**（同上。アプリ画面では検査ビン容量計－検査ビン残数量を算出して表示のみ行う） | 2026-09-09 版1.1で変更 |
| 21 | `field_1785401460` | 充填数量 | 数値計算 | **送信対象外**（同上。アプリ画面では納入数量－残数量－検査ビン使用量を算出して表示のみ行う。マイナス値チェックはアプリ内で算出した値を用いて実施する） | 2026-09-09 版1.1で変更 |

`_created_by`／`_created_at`／`_updated_by`／`_updated_at`はJustDB側のシステム項目のため送信対象外。

#### 6.2.2 ボンベ管理テーブル

| No | fieldId | 項目名 | 型 | 編集・変換規則 | 備考 |
|---|---|---|---|---|---|
| 1 | `field_1788918843` | ボンベID | - | JustDB側で自動採番。登録時は送信しない | |
| 2 | `field_1788918872` | 伝票ID | 手動採番（接頭語・接尾語なし） | 伝票管理テーブル登録後にGET取得した「伝票ID」（採番フィールド）から「番号」部分のみを取り出し、`["", "<番号>", ""]`（接頭語・接尾語は空文字）の配列形式でセットする | 親子連携キー。2026-09-09 版1.1で変更（手動採番フィールドの送信形式に合わせて修正） |
| 3 | `field_1788918913` | ボンベ記号 | 文字列 | 入力値（大文字英字）をそのまま送信 | |
| 4 | `field_1788918978` | ボンベ番号 | 文字列 | 入力値（数字のみ、最大6桁）をそのまま送信 | |
| 5 | `field_1788919100` | ボンベ容量 | 数値 | 「kg」を除いた数値のみ送信 | |
| 6 | `field_1788919183` | 引渡・引取 | 文字列 | 「引渡」または「引取」 | |
| 7 | `field_1788919234` | 検査ビン | 真偽値 | チェック有無（true／false） | |
| 8 | `field_1788919262` | 耐圧ビン | 真偽値 | チェック有無（true／false） | |
| 9 | `field_1788920501` | 販売店 | 文字列 | 伝票ヘッダーの「販売店」と同値をセット | |
| 10 | `field_1788920533` | 配送先 | 文字列 | 伝票ヘッダーの「配送先」と同値をセット | |
| 11 | `field_1788920544` | 日付 | 日付 | 伝票ヘッダーの「配送日」と同値をセット | |

`_created_by`／`_created_at`／`_updated_by`／`_updated_at`はJustDB側のシステム項目のため送信対象外。

### 6.3 Excel出力機能（2026-09-10 版1.2で追加）

メイン画面下部の「Excel出力」ボタンから出力用の別ウインドウを開き、期間（配送日）を指定して「伝票データ出力」「ボンベデータ出力」をそれぞれExcelファイル（.xlsx）として出力する。画面仕様は「11. 画面・操作仕様」を参照。

#### 6.3.1 データ取得方法

- JustDB「レコード複数件取得API」（`GET .../tables/{tableName}/records/?panelName=&filterName=&_{フィールド識別名}-0=&_{フィールド識別名}-1=&offset=&limit=`）を用いて、伝票管理テーブル・ボンベ管理テーブルからそれぞれ期間内のレコードを取得する。
- `panelName`・`filterName`には、JustDB管理画面側にあらかじめ作成済みのパネル・フィルター（配送日／日付に「以後」「以前」の2条件をAND設定したもの）の識別名を指定し、「動的指定可能検索」の仕組みでその2条件の値（開始日・終了日）をAPIリクエスト側から上書きすることで、サーバーサイドで期間絞り込みを行う。詳細・識別名は「6.3.4 フィルターの設置方針」を参照。
- 同APIは1回のリクエストで**最大100件（未指定時は10件）**までしか返さない仕様のため、`offset`／`limit=100`を使い、返却件数が100件未満になるまでページングを繰り返して該当レコードを全件取得する。

#### 6.3.2 伝票データ出力レイアウト（出力レイアウト_伝票データ.xlsx）

| 列 | 項目名 | 出力元 | 備考 |
|---|---|---|---|
| A | 配送日 | 伝票管理テーブル `field_20003` | |
| B | 伝票ID | 伝票管理テーブル `field_20002` | 採番フィールドの「番号」部分のみ |
| C | 販売店 | 伝票管理テーブル `field_20004` | |
| D | 配送先 | 伝票管理テーブル `field_20005` | |
| E | 管理方法 | 伝票管理テーブル `field_20006` | |
| F | 充填日 | 伝票管理テーブル `field_20007` | |
| G | 充填所 | 伝票管理テーブル `field_20008` | |
| H | 充填数量 | 伝票管理テーブル `field_1785401460` | JustDB側の数値計算フィールド（GET取得のみ可） |
| I | うち耐圧充填量 | 伝票管理テーブル `field_20021` | |
| J | 備考 | 伝票管理テーブル `field_20022` | 複数行の場合はセルの折り返し表示（WrapText）・上揃えを設定（2026-09-10 版1.4で追加） |
| K | 納入数量 | 伝票管理テーブル `field_20009` | |
| L | 残数量 | 伝票管理テーブル `field_20010` | |
| M | 検査ビン残数量 | 伝票管理テーブル `field_20018` | |
| N | 検査ビン容量計 | 伝票管理テーブル `field_1785401257` | JustDB側の数値計算フィールド |
| O | 検査ビン使用量 | 伝票管理テーブル `field_1785401362` | JustDB側の数値計算フィールド |
| P | 使用量計 | アプリ内計算 | 充填数量（H列）＋検査ビン使用量（O列） |
| Q | 年 | アプリ内計算 | 配送日（A列）の年部分 |
| R | 月 | アプリ内計算 | 配送日（A列）の月部分 |
| S | 日 | アプリ内計算 | 配送日（A列）の日部分 |

A〜O列はJustDB APIから取得した値をそのまま格納し、P〜S列（使用量計・年・月・日）はアプリ内で計算・分解した値を格納する。

全列の列幅は、ヘッダーおよび各データ行の表示内容を改行ごとに区切り、「半角文字＝1、全角文字＝2」換算で最長行の幅を算出し、その値（＋余白）を列幅として設定する（上限40・下限4、2026-09-14 版1.7）。ClosedXMLの列幅自動調整（`AdjustToContents`）は全角文字を半角文字と同じ幅で計算してしまうため採用していない。

#### 6.3.3 ボンベデータ出力レイアウト（出力レイアウト_ボンベデータ.xlsx）

| 列 | 項目名 | 出力元 | 備考 |
|---|---|---|---|
| A | ボンベID | ボンベ管理テーブル `field_1788918843` | 採番フィールドの「番号」部分のみ |
| B | 伝票ID | ボンベ管理テーブル `field_1788918872` | 採番フィールドの「番号」部分のみ |
| C | ボンベ記号 | ボンベ管理テーブル `field_1788918913` | |
| D | ボンベ番号 | ボンベ管理テーブル `field_1788918978` | |
| E | ボンベ容量 | ボンベ管理テーブル `field_1788919100` | |
| F | 引渡・引取 | ボンベ管理テーブル `field_1788919183` | |
| G | 検査ビン | ボンベ管理テーブル `field_1788919234` | |
| H | 耐圧ビン | ボンベ管理テーブル `field_1788919262` | |
| I | 販売店 | ボンベ管理テーブル `field_1788920501` | |
| J | 配送先 | ボンベ管理テーブル `field_1788920533` | |
| K | 日付 | ボンベ管理テーブル `field_1788920544` | 伝票ヘッダーの配送日と同値（登録時にシステムが自動セット） |
| L | 年 | アプリ内計算 | 日付（K列）の年部分 |
| M | 月 | アプリ内計算 | 日付（K列）の月部分 |
| N | 日 | アプリ内計算 | 日付（K列）の日部分 |
| O | 集計用 | アプリ内固定値 | 常に `1` を格納 |

列幅の算出方法は6.3.2と同様（「半角文字＝1、全角文字＝2」換算、上限40・下限4、2026-09-14 版1.7）。

A〜K列はJustDB APIから取得した値をそのまま格納し、L〜O列（年・月・日・集計用）はアプリ内で計算・付与した値を格納する。

#### 6.3.4 フィルターの設置方針

期間（配送日／日付）による絞り込みは、JustDB管理画面側にあらかじめ用意されたフィルターを利用し、サーバーサイド（JustDB側）で行う方針とした（2026-09-10 版1.3で、当初のアプリ側絞り込み方式から変更）。

**JustDB APIが提供する絞り込み手段**

- 「フィールド値検索」（`_{fieldName}=値` によるクエリパラメータ指定）：完全一致（である）のみに対応しており、対応フィールド種類は文字列（1行）・数値・採番（全体）・枝番・メールに限られる。日付時刻フィールドは対象外であり、この方式では期間（範囲）指定はできない。
- 「動的指定可能検索」：JustDB管理画面上で事前に作成した「パネル」と「フィルター」（配送日／日付に「以後」「以前」の2条件を設定したもの）を用意し、APIリクエストで `panelName`・`filterName` とともに条件値を上書き指定することで、日付範囲によるサーバーサイド絞り込みが可能。

**採用した方式（動的指定可能検索）**

JustDB管理画面側に以下のパネル・フィルターが作成済みであることを確認し、これを利用する。各フィルターは配送日／日付に対して「以後」「以前」の2条件（絶対指定、AND条件）を設定したもので、APIからは1件目の条件（以後）に開始日、2件目の条件（以前）に終了日を上書き指定する。

| 対象テーブル | パネル識別名 | フィルター識別名 | 対象フィールド | 条件構成 |
|---|---|---|---|---|
| 伝票管理（配送日） | `panel_1785400222` | `filter_1789019365` | `field_20003`（配送日） | 以後（-0）／以前（-1）、絶対指定、AND |
| ボンベ管理（日付） | `panel_1788918749` | `filter_1789019750` | `field_1788920544`（日付） | 以後（-0）／以前（-1）、絶対指定、AND |

上記の識別名は `appsettings.json` の `JustDb.SlipPanelName`／`JustDb.SlipFilterName`／`JustDb.CylinderPanelName`／`JustDb.CylinderFilterName` に設定する。実際にAPIへテストリクエストを送信し、指定した期間のレコードのみが返却されることを確認済みである。

**不採用とした方式（アプリ側絞り込み）**

初期実装（版1.2）では、JustDB管理画面側でのパネル・フィルター作成という運用上の準備が整っていなかったため、レコード複数件取得APIで全件を取得（ページング対応）した上でアプリ内で期間の開始日・終了日と配送日／日付を比較して絞り込む方式を採用していた。想定データ量（1日5〜10件程度）では実用上問題ない速度だったが、パネル・フィルターが用意されたことでJustDB側フィルターへ切り替え、通信量・処理時間をより抑える方式とした。

**留意事項**

- パネル名・フィルター識別名がJustDB管理画面側で変更・削除された場合、Excel出力機能が正しく動作しなくなる。設定を変更する際は `appsettings.json` の対応する値も合わせて更新すること。
- フィルターの条件構成（以後／以前の順序、絶対指定であること）が変わると、APIへの上書きパラメータの対応関係（`-0`／`-1`）がずれるため、フィルターを修正する場合は本節の内容と整合しているか確認すること。

### 6.4 参照・編集・削除機能（2026-09-11 版1.6で追加）

メイン画面の「参照・編集」ボタンから別ウインドウ（参照・編集画面）を開き、登録済みの伝票を伝票IDで検索して内容を確認・修正し、「変更」ボタンでまとめて更新できる。また「伝票削除」ボタンで伝票ごと削除できる。画面仕様は「11. 画面・操作仕様」を参照。

#### 6.4.1 検索（伝票ID指定）

- JustDB「フィールド値検索」（`GET .../tables/{tableName}/records/?_{fieldId}=値`）を用いる。完全一致検索のみに対応し、対応フィールド種類は文字列（1行）・数値・採番（全体）・枝番・メールに限られる（6.3.4節参照）。伝票管理テーブルの伝票ID（`field_20002`、採番型）・ボンベ管理テーブルの伝票ID（`field_1788918872`、枝番型）はいずれも対応フィールド種類に該当し、実際にAPIへ読み取り専用のテストリクエストを送信して動作（`recordId`を含むレコードが返却されること）を確認済みである。
- 入力された伝票IDで伝票管理テーブル・ボンベ管理テーブルの両方を検索し、該当する伝票ヘッダー（1件）とボンベ情報明細（0件以上）を画面に読み込む。
  - 該当する伝票ヘッダーが0件の場合：「該当する伝票が見つかりませんでした」と表示し、画面をクリアする。
  - 該当する伝票ヘッダーが2件以上見つかった場合：データ不整合の可能性があるとして警告を表示し、先頭の1件を表示する。
- 検索結果読み込み後のみ、ヘッダー項目・ボンベ情報明細の編集、「変更」「伝票削除」ボタンの操作が可能になる（検索前は編集エリアを無効化する）。
- 販売店・配送先・充填所が現在のローカルマスタ一覧に存在しない場合（マスタ更新後の履歴データ等）、表示が消えないよう一時的に選択肢へ追加したうえで表示する。

#### 6.4.2 変更（レコード更新）

- 「変更」ボタン押下時、伝票ヘッダー・ボンベ情報明細のバリデーション（5.2節・7.2節の入力チェック・自動計算・マイナス値チェック）を登録処理と同様に実施したうえで、確認ダイアログ（更新・追加・削除の件数を表示）を表示する。
- 編集内容は「変更」ボタンを押すまでJustDB側には反映しない。ボンベ情報明細のゴミ箱アイコンによる行削除も、画面上から消えるのみで実際の削除は保留（ステージング）される。
- 「変更」確定後、検索時点のボンベ情報明細（`recordId`保持）と画面上の現在の行を比較し、以下のとおり差分適用する。
  - 検索時に存在し、画面上からなくなった行 → レコード削除（DELETE）
  - `recordId`を保持したまま画面上に残っている行 → レコード更新（UPDATE／PUT）
  - `recordId`を持たない（新規追加された）行 → レコード挿入（INSERT／POST）
- 伝票ヘッダーの更新には`UpdateRecordAsync`（PUT、`{"records":[{"recordId":…,"record":{…}}]}`）を用いる。POST（挿入）・DELETE（削除）が同じ配列ラッパー形式であることから類推した実装であり、`api_help.pdf`上での契約確認はできていない。APIがエラーを返す場合はレスポンスのエラーメッセージをそのまま画面表示する。
- 伝票ヘッダーの更新に失敗した場合はボンベ情報明細の更新処理を行わない。ボンベ情報明細の一部更新・追加・削除に失敗した場合は、失敗した項目を一覧表示したうえでサーバー側の現在の状態を再検索・再表示し、内容を確認できるようにする。
- すべてエラーなく完了した場合は、画面をクリアし、続けて次の伝票をすぐに検索・編集できる状態にする（2026-09-11 版1.6で追加）。

#### 6.4.3 伝票削除

- 「伝票削除」ボタンは「変更」とは独立した操作であり、確認ダイアログ（この操作は取り消せない旨を明示）の後、即時に削除を実行する。
- 削除時点でJustDB側に存在する、紐づくボンベ情報明細を検索し直したうえで全件削除し、続けて伝票ヘッダーを削除する。
- ボンベ情報明細の削除に一部失敗した場合、伝票ヘッダーは削除せずに中断し、システム課へ連絡するよう案内する。

---

## 7. 処理仕様（業務ロジック）

### 7.1 処理の全体フロー

1. 担当者がアプリを起動し、伝票情報・ボンベ情報を入力する
2. 「登録」ボタンを押下する
3. 必須項目（配送日、販売店、配送先、充填日、充填所、管理方法、納入数量、残数量、および各ボンベ情報行の引渡／引取）の入力チェックを行う。未入力の必須項目があれば、その項目名を提示するエラーメッセージを表示し、処理を中断する
4. ボンベ情報明細のうち、全項目が空欄の行を登録対象から除外する
5. 検査ビン容量計・検査ビン使用量・充填数量を自動計算する
6. 計算後の数値項目（残数量、充填数量等）がマイナスとなる場合、警告メッセージを表示し登録を中断する
6-2. 登録内容の確認ダイアログ（配送日・販売店・配送先・充填日・充填所・ボンベ情報明細件数を表示。ボンベ情報明細が0件の場合はその旨を明示）を表示する。「いいえ」が選択された場合は入力内容を保持したまま処理を中断する（2026-09-10 版1.5で追加）
7. 伝票ヘッダー情報をJustDB伝票管理テーブルへPOST登録する
8. 登録結果の`recordId`を用いて対象レコードをGET取得し、自動採番された「伝票ID」の値を取得する
9. 登録対象のボンベ情報明細を1件ずつ、取得した伝票ID・伝票ヘッダーの販売店／配送先／配送日を付与してJustDBボンベ管理テーブルへPOST登録する
10. 明細登録中にエラーが発生した場合、登録済みの明細および伝票ヘッダーをDELETEで削除しロールバックする。ロールバック自体が失敗した場合は、システム課へ連絡するよう案内メッセージを表示する
11. すべての登録が正常に完了した場合、エラーメッセージを表示し、入力フォームの内容をクリア（初期化）し、次の伝票をすぐに入力できる状態にする

### 7.2 主要ロジック詳細

| 区分 | 条件 | 処理内容 |
|---|---|---|
| 検査ビン容量計 | 常時 | 検査ビン容量1×本数1＋検査ビン容量2×本数2＋検査ビン容量3×本数3（各容量は「kg」を除いた数値で計算） |
| 検査ビン使用量 | 常時 | 検査ビン容量計－検査ビン残数量 |
| 充填数量 | 常時 | 納入数量－残数量－検査ビン使用量 |
| 配送先候補の絞り込み | 「販売店」選択時 | 配送先マスタから、選択された「販売店」に紐づく「配送先」候補を抽出し、配送先プルダウンに表示する |

### 7.3 マスタ参照仕様

| 参照マスタ名 | キー項目 | 参照タイミング | 優先順位・フォールバック |
|---|---|---|---|
| 配送先マスタ | 販売店（名称） | 「販売店」選択時 | 該当なし（名称による完全一致のみ） |
| 充填所マスタ | なし（固定候補リスト） | 画面表示時 | 該当なし |

---

## 8. 除外・特殊ケースの扱い

| No | 除外条件 | 理由 |
|---|---|---|
| 1 | ボンベ情報明細のうち、全項目が空欄のままの行 | 未入力行は入力ミス・意図しない空行とみなし、登録対象外とするため |
| 2 | 残数量・充填数量等の数値項目がマイナス値となる入力 | 業務上あり得ない値であり、データ不整合を防ぐため。現時点では警告表示のうえ登録不可とし、実運用開始後に不具合があれば見直す |

---

## 9. エラー処理・異常系仕様

| No | 異常ケース | 検知方法 | 挙動（継続/中断） | ログ出力内容 |
|---|---|---|---|---|
| 1 | 必須項目未入力のまま登録操作 | 登録ボタン押下時の入力チェック | 中断（未入力の必須項目名を提示するエラーメッセージを表示） | エラー内容・未入力項目名 |
| 2 | 数値項目（残数量・充填数量等）がマイナスとなる | 自動計算後の値チェック | 中断（警告表示、登録不可） | 警告内容・該当項目の入力値 |
| 3 | ボンベ番号に数字以外の文字が入力された | 入力時バリデーション | 中断（エラー表示、登録不可） | エラー内容・入力値 |
| 4 | 引渡／引取が未選択のまま登録操作 | 登録ボタン押下時の入力チェック | 中断（必須選択エラーを表示） | エラー内容 |
| 5 | JustDB API通信エラー（レート制限超過・通信断等） | APIレスポンスのエラー判定、および接続不可・タイムアウト等の通信例外の捕捉 | 中断（エラーメッセージを表示し、再登録を促す。自動リトライは行わない） | エラーコード・エラーメッセージ |
| 6 | ボンベ情報明細登録中に一部の明細が登録失敗 | APIレスポンスのエラー判定 | 中断（登録済みの明細および伝票ヘッダーをDELETEでロールバック。ロールバック失敗時はシステム課への連絡を案内） | エラー内容・ロールバック結果 |
| 7 | Excel出力用のパネル／フィルター設定（appsettings.json）が未設定、またはJustDB管理画面側で該当パネル・フィルターが見つからない | 出力処理実行時のAPIレスポンス判定（事前チェック含む） | 中断（分かりやすいエラーメッセージを表示） | エラー内容 |
| 8 | 参照・編集画面で、入力した伝票IDに該当する伝票が見つからない | フィールド値検索結果が0件 | 中断（「該当する伝票が見つかりませんでした」と表示し、画面をクリア） | 検索した伝票ID |
| 9 | 参照・編集画面で、入力した伝票IDに該当する伝票が2件以上見つかる（データ不整合） | フィールド値検索結果が2件以上 | 継続（警告表示のうえ先頭の1件を表示） | 検索した伝票ID・該当件数 |
| 10 | 参照・編集画面の「変更」で、伝票ヘッダーのレコード更新（PUT）が失敗する | APIレスポンスのエラー判定、および通信例外の捕捉 | 中断（エラーメッセージを表示。ボンベ情報明細の更新は行わない） | エラーコード・エラーメッセージ |
| 11 | 参照・編集画面の「変更」で、ボンベ情報明細の一部の更新・追加・削除が失敗する | APIレスポンスのエラー判定 | 継続（伝票ヘッダーは更新済みの状態とし、失敗した項目を一覧表示したうえでサーバー側の現在の状態を再表示。ロールバックは行わない） | エラー内容・失敗した明細の情報 |
| 12 | 「伝票削除」で、紐づくボンベ情報明細の一部削除が失敗する | APIレスポンスのエラー判定 | 中断（伝票ヘッダーは削除せず、システム課への連絡を案内） | エラー内容・該当recordId |

**2026-09-10 版1.4での修正について**

伝票ヘッダー登録処理（`RegisterAsync`）は、当初JustDB APIがエラーレスポンスを返すケース（`JustDbApiException`）のみを捕捉しており、通信断・タイムアウトなど**それ以外の例外は捕捉されず利用者に一切通知されない**不具合があった（`async Task`のコマンドはTaskをawaitせず実行されるため、内部で捕捉されなかった例外は静かに失われる）。該当箇所への`catch (Exception)`の追加に加え、登録処理・Excel出力処理それぞれの最上位にも予期しない例外を最終的に捕捉する層を追加し、いかなる例外発生時にも利用者へエラーメッセージが表示されるようにした。

---

## 10. 非機能要件

- **想定データ件数**：1日あたり5〜10件程度
- **実行頻度・実行タイミング**：随時（担当者が都度手動起動）
- **同時利用人数**：1人
- **排他制御・多重起動防止**：該当なし（同時利用は1人を想定しているため）
- **ログ出力方針**：操作履歴およびエラー内容を記録する。保存期間は無期限（全期間保持）
- **セキュリティ**：APIキーはソースコード・設計書等への直書きを避け、環境変数等の設定で管理する
- **障害時の運用**：ロールバック失敗等によりJustDB上にデータ不整合が生じる可能性がある場合、システム課へ連絡する運用とする

---

## 11. 画面・操作仕様（該当する場合のみ）

- **起動方法**：担当者がアプリを手動起動し、画面上で伝票情報・ボンベ情報を入力する
- **画面構成**：提供画像（入力画面SS.png）のレイアウトに準拠する
  - 画面左側：伝票ヘッダー入力エリア（配送日、販売店、配送先、管理方法、充填日、充填所、納入数量、残数量、検査ビン容量／本数1〜3、検査ビン容量計、検査ビン残数量、検査ビン使用量、充填数量、うち耐圧充填量、備考）
    - 備考欄（複数行入力）は文字を上揃えで表示する（2026-09-10 版1.4で修正。修正前は全TextBox共通スタイルの影響で縦方向中央揃えになっていた）
  - 画面右側：ボンベ情報明細一覧（ボンベ記号、ボンベ番号、容量、引渡／引取、検査ビン、耐圧ビン）
    - 初期表示は1行のみとし、「項目の追加」ボタンで必要な行数を追加する（2026-09-09 版1.1で変更。初版では5行を初期表示としていた）
  - 「項目の追加」ボタンでボンベ情報明細行を追加する
  - 各明細行のゴミ箱アイコンで行を削除する
  - 「登録」ボタン押下で、7章の登録処理を実行する
    - 入力チェック・自動計算・マイナス値チェックを通過すると、登録確認ダイアログ（はい／いいえ、既定はいいえ）を表示する（2026-09-10 版1.5で追加）
    - 確認ダイアログには配送日・販売店・配送先・充填日・充填所とボンベ情報明細の件数を表示する
    - ボンベ情報明細が0件（未入力）のまま登録しようとした場合は、警告アイコンとともに「※ ボンベ情報明細は0件（未入力）のまま登録します。」と明示し、入力漏れに気付けるようにする
    - 「いいえ」を選択した場合は入力内容を保持したまま登録処理を中断する
- 登録が正常に完了した場合、入力フォームの内容をすべてクリア（初期化）し、次の伝票をすぐに入力できる状態にする
- **Excel出力画面（2026-09-10 版1.2で追加）**
  - メイン画面下部に「Excel出力」ボタンを配置する
  - 「Excel出力」ボタン押下で、別ウインドウ（Excel出力画面）をモーダル表示する
  - Excel出力画面には以下を配置する
    - 期間（配送日）の絞り込み用入力欄（開始日・終了日、初期値は本日）
    - 「伝票データ出力」ボタン
    - 「ボンベデータ出力」ボタン
  - 各出力ボタン押下時の流れ：期間の入力チェック → 保存先ファイル名の指定（名前を付けて保存ダイアログ、既定ファイル名は「伝票データ_開始日_終了日.xlsx」／「ボンベデータ_開始日_終了日.xlsx」）→ JustDBからのデータ取得・期間絞り込み → Excelファイル生成・保存 → 出力件数を通知するメッセージを表示
  - 出力データのレイアウトは「6.3 Excel出力機能」を参照
- **参照・編集画面（2026-09-11 版1.6で追加）**
  - メイン画面下部に「参照・編集」ボタンを配置する
  - 「参照・編集」ボタン押下で、別ウインドウ（参照・編集画面）をモーダル表示する
  - 参照・編集画面には以下を配置する
    - 伝票ID入力欄＋「検索」ボタン（検索後は表示中の伝票IDをあわせて表示）
    - 伝票ヘッダー入力エリア（メイン画面の登録フォームと同じ項目構成）
    - ボンベ情報明細一覧（メイン画面と同じ列構成。「項目の追加」ボタン、各行のゴミ箱アイコンによる削除）
    - 「伝票削除」ボタン、「変更」ボタン
  - 検索前はヘッダー入力エリア・ボンベ情報明細一覧・「変更」「伝票削除」ボタンを無効化する
  - 「検索」押下時の流れ：伝票ID入力チェック → JustDBフィールド値検索（伝票管理・ボンベ管理の両テーブル） → 該当なしの場合はメッセージ表示のうえ画面クリア、該当ありの場合は内容を画面に表示し編集可能にする
  - 「変更」押下時の流れ：入力チェック・自動計算・マイナス値チェック（7章に同じ）→ 確認ダイアログ（更新・追加・削除件数を表示）→ 伝票ヘッダーのレコード更新（PUT）→ ボンベ情報明細の差分適用（追加＝INSERT、変更＝UPDATE、削除＝DELETE）→ 完了メッセージ表示
    - エラーなく完了した場合は画面をクリアし、続けて次の伝票をすぐに検索・編集できる状態にする
    - 一部失敗した場合はエラー内容を表示し、サーバー側の現在の状態を再表示する
  - 「伝票削除」押下時の流れ：確認ダイアログ（取り消せない旨を明示）→ 紐づくボンベ情報明細を全件削除 → 伝票ヘッダーを削除 → 完了メッセージ表示のうえ画面をクリア
  - 処理内容の詳細は「6.4 参照・編集・削除機能」を参照

---

## 12. テスト観点（実装後の確認ポイント）

| No | テスト観点 | 期待結果 |
|---|---|---|
| 1 | 必須項目（配送日、販売店、配送先、充填日、充填所、管理方法、納入数量、残数量）を空欄のまま登録操作を行う | 未入力項目名を提示するエラーメッセージが表示され、登録が中断される |
| 2 | 残数量に納入数量を超える値を入力する | 充填数量がマイナスとなり警告が表示され、登録が中断される |
| 3 | ボンベ番号に英字や記号を入力する | エラーメッセージが表示され、登録できない |
| 4 | ボンベ番号に7桁以上の値を入力する | 6桁を超える入力ができない、またはエラーとなる |
| 5 | 引渡／引取を未選択のまま登録操作を行う | 必須選択エラーが表示され、登録が中断される |
| 6 | ボンベ情報明細のうち一部の行を空欄のまま登録操作を行う | 空欄行は登録対象から除外され、入力済みの行のみ登録される |
| 7 | 「販売店」選択後の「配送先」プルダウンを確認する | 選択した販売店に紐づく配送先候補のみが表示される |
| 8 | 検査ビン容量計・検査ビン使用量・充填数量の自動計算結果を確認する | 各項目が定義された計算式どおりに自動算出される |
| 9 | 正常登録後のフォーム状態を確認する | 入力項目がすべてクリア（初期化）される |
| 10 | ボンベ情報登録中に通信エラーを意図的に発生させる | 登録済みの明細・伝票ヘッダーがロールバック（削除）され、エラーメッセージが表示される |
| 11 | JustDB API通信エラー（レート制限超過等）を発生させる | エラーメッセージが表示され、再登録を促す表示となる |
| 12 | 参照・編集画面で、既存の伝票IDを検索する | 該当する伝票ヘッダー・ボンベ情報明細が画面に表示される |
| 13 | 参照・編集画面で、存在しない伝票IDを検索する | 「該当する伝票が見つかりませんでした」と表示される |
| 14 | 参照・編集画面で項目を変更し「変更」を押す | JustDB側の値が更新され、完了メッセージ表示後に画面がクリアされる |
| 15 | 参照・編集画面でボンベ情報明細を1行追加して「変更」を押す | JustDBボンベ管理テーブルに新規レコードが追加される |
| 16 | 参照・編集画面でボンベ情報明細を1行削除（ゴミ箱アイコン）して「変更」を押す | 該当レコードがJustDBボンベ管理テーブルから削除される |
| 17 | 参照・編集画面で「伝票削除」を実行する | 伝票ヘッダー・紐づくボンベ情報明細がすべてJustDBから削除される |

---

## 13. 未決事項・検討課題

### 13.1 実装ブロッカー（解決必須・未解決のまま実装着手不可）

該当なし

### 13.2 運用・後決め事項（実装には影響しない）

該当なし

---

## 14. 別紙・参考資料

- 配送先マスタ_20260908.xlsx（配送先マスタ、95件：販売店／配送先）
- 充填所マスタ_20260908.xlsx（充填所マスタ、6件）
- テーブル_フィールド情報.xlsx（JustDB伝票管理・ボンベ管理テーブルのtableName／fieldId一覧）
- 入力画面SS.png（画面レイアウト参考画像）
- 出力レイアウト_伝票データ.xlsx（Excel出力機能・伝票データのレイアウト定義。2026-09-10追加）
- 出力レイアウト_ボンベデータ.xlsx（Excel出力機能・ボンベデータのレイアウト定義。2026-09-10追加）
- api_help.pdf（JustDB「API連携利用ヘルプ」公式仕様書。2026-09-10入手）
