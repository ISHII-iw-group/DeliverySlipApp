<Window x:Class="DeliverySlipApp.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:DeliverySlipApp.Converters"
        mc:Ignorable="d"
        Title="Excel出力" Height="260" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Meiryo UI" FontSize="13">

    <Window.Resources>
        <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <Style TargetType="TextBlock" x:Key="FieldLabel">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Background="#DCE6EE" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}">
        <StackPanel Margin="20">
            <TextBlock Text="出力する期間（配送日）を指定してください。" Margin="0,0,0,16" TextWrapping="Wrap"/>

            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="期間" Style="{StaticResource FieldLabel}"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding StartDate}"/>
                <TextBlock Grid.Column="2" Text="〜" Style="{StaticResource FieldLabel}" Margin="8,0"/>
                <DatePicker Grid.Column="3" SelectedDate="{Binding EndDate}"/>
            </Grid>

            <Button Content="伝票データ出力" Padding="12,10" Margin="0,0,0,10"
                    FontSize="14" Command="{Binding ExportSlipCommand}"/>
            <Button Content="ボンベデータ出力" Padding="12,10"
                    FontSize="14" Command="{Binding ExportCylinderCommand}"/>
        </StackPanel>
    </Grid>
</Window>
